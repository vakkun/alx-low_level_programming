#include <stdio.h>
/**
 * main - Entry point
 * void - empty
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;
	for (char 'a'; c < 'z'; c++)
	{
		putchar(c);
	}
	putchatr('\n');
	return (0);
}
